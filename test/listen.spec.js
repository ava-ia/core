'use strict';

import { assert, expect, should } from 'chai';

describe('.listen()', () => {

  it('An intent could be unsuccesful', async () => {
    // @TODO: Should be in `listen.spec.js`
  });

  it('group of intents could be unsuccesful', async () => {
    // @TODO: Should be in `listen.spec.js`
  });

});
