'use strict';

import { expect } from 'chai';
import { factoryActions } from '../../src/helpers';

describe('Helper: factoryActions', () => {

  it('Up & Running', async () => {
    expect(factoryActions).to.be.ok;
  });

});
