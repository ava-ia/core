'use strict';

import { expect } from 'chai';
import { factoryIntents } from '../../src/helpers';

describe('Helper: factoryIntents', () => {

  it('Up & Running', async () => {
    expect(factoryIntents).to.be.ok;
  });

});
