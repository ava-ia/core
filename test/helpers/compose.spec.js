'use strict';

import { expect } from 'chai';
import { compose } from '../../src/helpers';

describe('Helper: compose', () => {

  it('Up & Running', async () => {
    expect(compose).to.be.ok;
  });

});
